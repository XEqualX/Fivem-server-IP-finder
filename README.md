# Fivem-Ip-grabber
A simple IP grabber for fivem. all it needs is the connect code. It uses a custom API to fetch server info like the recourses, IP, player count and a bit more. it uses libcpr/cpr.h for networking and nlohmann/json.hpp for json handling. 
